const validators = {
  isNaN: ( data ) =>{  console.log("am i running?")
    return isNaN( data )},
    isEmailValid:(data) =>{
      console.log()
    }
}
  // ... more additional validators;
export default validators;